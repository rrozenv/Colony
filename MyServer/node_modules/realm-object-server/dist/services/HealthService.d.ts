/// <reference types="express" />
import { Request as ExpressRequest, Response as ExpressResponse } from "express";
export declare class HealthService {
    private started;
    live(): void;
    getHealth(request: ExpressRequest, res: ExpressResponse): void;
}
